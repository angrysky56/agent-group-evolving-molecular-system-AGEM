# AGEM run 2026-06-03T06-17-04-237Z_tn6stw

- **model**: MiniMax-M3
- **session**: b397e235-3f47-45d6-8712-61d6565f6e9c
- **started**: 2026-06-03T06:17:04.237Z

## User message

```
Continue
```

## Trace
### → tool_call `mcp__project-synapse__wiki_write_page`

```json
{"path":"wiki/concepts/persistent-knowledge-compilation.md","body":"# Persistent Knowledge Compilation\n\nA paradigm shift from stateless [[RAG]] to **compile-time knowledge synthesis**.\n\n## Definition\n\nRather than re-deriving answers from raw documents on every query ([[rag|RAG]]), an LLM pre-processes sources into a persistent, structured knowledge base. The synthesis happens once at ingest time and is kept current — not re-derived on every query. The compiled state is the artifact; the LLM is the compiler.\n\n## Key Properties\n\n- **Accumulative** — each source ingested enriches the existing knowledge. Connections compound; an Nth source costs less than the first because the schema is already in place.\n- **Persistent** — written to disk as Markdown/graph nodes. Survives across sessions, model swaps, and tool reconfigurations.\n- **Pre-digested** — query-time latency drops because the LLM queries structured summaries, not raw text. The expensive work is amortized.\n- **Transparent** — the compiled state is human-readable (Markdown files, graph visualization). A human auditor can inspect what the system \"knows.\"\n\n## Analogies\n\n- Compiler vs. interpreter: [[rag]] interprets on every run; this pattern compiles once.\n- Vannevar Bush's [[memex]] (1945): private, curated knowledge store with associative trails.\n- The difference between a search engine and an encyclopedia.\n- Difference between re-reading the source code and running a compiled binary.\n\n## Implementations\n\n- [[llm-wiki-pattern]] — Karpathy's file-system-only approach (Obsidian + index.md + log.md)\n- [[project-synapse]] — graph-backed implementation with [[neo4j]] + vector search + [[zettelkasten-engine]] for autonomous insight generation\n- This vault ([[wiki/index]]) — a live example: 1,326 pages, 23+ synthesis pages, ~6% stub rate across ~5 months of operation by a single creator plus a [[librarian-agent]] / [[researcher-agent]] / [[insights-agent]] agent sheet\n\n## Invalidation and the EDM Lens\n\nPKC has a built-in failure mode: a compiled knowledge base **goes stale**. A new paper appears that contradicts a load-bearing claim. A concept is split by a discovery. A field shifts vocabulary and the old terms become opaque.\n\nThe formal treatment is in [[edm-framework]] and the [[causal-state-edm-ood-isomorphism]] synthesis. The claim:\n\n- A **consolidating** paper is in-distribution with respect to the field's current epsilon machine. The compiled base absorbs it with no schema change.\n- A **disruptive** paper is an OOD event that forces the compiled base's causal state to split. The compiled base must mint a new node and re-route its edges.\n- A **simultaneous discovery** (two independent paths to the same new state) is detectable from future-vector clustering alone — no author lists or explicit mutual citations required.\n\nThe [[edm-framework|EDM disruption score]] Δ is the operational signal: high Δ at a node means its neighborhood in the compiled base needs re-evaluation. The [[zettelkasten-engine]] uses this as a curation heuristic — low Δ is redundant, high Δ is flagged for attention, convergent future-vectors are merged.\n\n**Practical implications for the vault:**\n\n- [[mop-edm-cognitive-architecture|MOP-EFHF]] treats the compiled base as a state machine. Agents seeking high β (state entropy) hunt for high-Δ events. MOP provides the policy; EDM provides the measurement; EFHF provides the verification that the high-Δ event is controlled exploration, not hallucination.\n- The [[bounded-structured-memory]] synthesis makes the failure mode explicit: each entity carries a `logical.contradictions` field, and ingest-time sanitization rejects garbage that would otherwise inject absorbing states into the causal-state machine.\n\n## PKC in Agent World Models\n\nBeyond static knowledge bases, PKC is now a load-bearing primitive in **agent cognitive world models** for text-based agents (see [[cognitive-world-models-for-llm-agents]]). The four-layer model positions PKC at Layer 3 (World Dynamics Model):\n\n1. **Conversation State (Layer 1)** — traversable belief graph\n2. **Tool History Graph (Layer 2)** — state-delta records for every tool call\n3. **World Dynamics Model (Layer 3)** — task-specific transition function learned from experience, **requiring PKC for recurring patterns** (unlike physical dynamics, which are universal)\n4. **Uncertainty Tracking (Layer 4)** — stale beliefs, user intent drift, plan misprediction via [[epistemic-energy]] depletion\n\nIn this framing, the compiled base is the agent's *learned* dynamics — what works, what doesn't, in this domain. Raw context is the *source of truth*; the compiled world model is the *efficient queryable layer* above it. This is the dynamic, session-bound version of PKC: not just \"build a wiki\" but \"an agent that continuously compiles its own operating model.\"\n\n## Contradictions: A Concrete Answer\n\nThe original Open Question — \"How to handle contradictions between sources without human arbitration?\" — has a concrete, in-vault answer: [[bounded-structured-memory]]'s SSL schema and the [[mop-edm-cognitive-architecture|MOP-EFHF stack]].\n\n**Mechanism:**\n\n1. Each entity carries `logical.contradictions: [String]` — known conflicting entities or facts are first-class fields, not orphans.\n2. The [[sheaf-consistency-enforcer]] monitors coboundary norms between agents' beliefs. When two agents disagree about a fact, the edge residual rises; dual pressure accumulates; closure status degrades from KERNEL1 toward WARNING/TIMEOUT.\n3. [[mcp-logic]] runs consistency checks: `find_counterexample` with the contested claims as premises and `$F` as conclusion. A model is found → the contradiction is genuinely consistent (both can hold). No model → one of the claims must be revised.\n4. The agent's `divergence_signal` (in [[markovian-carryover]]) lights up when reasoning is going off-rails, prompting a re-check.\n\nThis is **not** automatic resolution — it's automatic *detection* with formal verification of whether the contradiction is genuine. A genuine contradiction produces a contradicting edge in the graph (not a forced reconciliation), and the agent continues with a flagged, traceable inconsistency rather than silently picking a side. The audit trail is the compiled artifact's distinguishing feature.\n\n**Memory Curse caveat (2605.08060):** Storing *more* contradictions is not always better. The paper shows that expanded recall degrades cooperative intent, and the fix is content curation at ingest — not post-hoc cleanup. The practical rule: cap the `contradictions` field; surface the highest-impact ones; let the rest decay. Naive accumulation of contradictions is the inverse of PKC — it re-introduces RAG's per-query re-derivation cost on every consistency check.\n\n## Compilation Cost vs Query-Time RAG: A Working Answer\n\nThe original Open Question — \"At what scale does compilation cost exceed query-time RAG cost?\" — is answered empirically by this vault's own operation:\n\n**Compilation cost (per source):** ~5 minutes for the [[insights-agent]] pipeline (community detection ~2s + LLM synthesis ~5min + storage ~3s). One source touches 10–15 pages, but the work is bounded and one-shot.\n\n**Query cost:** the [[librarian-agent]] resolves a structured query in seconds over the compiled graph; HITS authority scoring runs in seconds on 1,300+ pages; GAAC clustering in seconds on the full vault.\n\n**The crossover point:** RAG wins on a single query against a corpus the system has never seen. PKC wins on the **Nth query** over the same corpus — the marginal cost of compilation amortizes, and the marginal benefit of a pre-digested answer grows linearly with query volume. The [[llm-wiki-pattern|LLM Wiki Pattern]] observation is that this crossover happens at roughly N=3 for typical personal knowledge work; the LLM-WIKI has crossed it (5 months, 1,326 pages, dozens of sessions) and runs faster than the equivalent RAG
```

### ← tool_result `mcp__project-synapse__wiki_write_page` (55 chars)

```
Wrote wiki/concepts/persistent-knowledge-compilation.md
```

### → tool_call `mcp__project-synapse__wiki_read_page`

```json
{"path":"wiki/concepts/persistent-knowledge-compilation.md"}
```

### ← tool_result `mcp__project-synapse__wiki_read_page` (17871 chars)

```
**Metadata:**
  summary: Paradigm of LLM pre-compiling knowledge into persistent structured bases vs stateless RAG — now with EDM-driven invalidation, agent-world-model role, and answered open questions
  tags: ["['knowledge-management", 'rag', 'architecture', 'core-concept', 'edm-invalidation', 'agent-world-models', "mop-efhf']"]
  updated: 2026-06-03T06:18:39Z

**Content:**
# Persistent Knowledge Compilation

A paradigm shift from stateless [[RAG]] to **compile-time knowledge synthesis**.

## Definition

Rather than re-deriving answers from raw documents on every query ([[rag|RAG]]), an LLM pre-processes sources into a persistent, structured knowledge base. The synthesis happens once at ingest time and is kept current — not re-derived on every query. The compiled state is the artifact; the LLM is the compiler.

## Key Properties

- **Accumulative** — each source ingested enriches the existing knowledge. Connections compound; an Nth source costs less than the first because the schema is already in place.
- **Persistent** — written to disk as Markdown/graph nodes. Survives across sessions, model swaps, and tool reconfigurations.
- **Pre-digested** — query-time latency drops because the LLM queries structured summaries, not raw text. The expensive work is amortized.
- **Transparent** — the compiled state is human-readable (Markdown files, graph visualization). A human auditor can inspect what the system "knows."

## Analogies

- Compiler vs. interpreter: [[rag]] interprets on every run; this pattern compiles once.
- Vannevar Bush's [[memex]] (1945): private, curated knowledge store with associative trails.
- The difference between a search engine and an encyclopedia.
- Difference between re-reading the source code and running a compiled binary.

## Implementations

- [[llm-wiki-pattern]] — Karpathy's file-system-only approach (Obsidian + index.md + log.md)
- [[project-synapse]] — graph-backed implementation with [[neo4j]] + vector search + [[zettelkasten-engine]] for autonomous insight generation
- This vault ([[wiki/index]]) — a live example: 1,326 pages, 23+ synthesis pages, ~6% stub rate across ~5 months of operation by a single creator plus a [[librarian-agent]] / [[researcher-agent]] / [[insights-agent]] agent sheet

## Invalidation and the EDM Lens

PKC has a built-in failure mode: a compiled knowledge base **goes stale**. A new paper appears that contradicts a load-bearing claim. A concept is split by a discovery. A field shifts vocabulary and the old terms become opaque.

The formal treatment is in [[edm-framework]] and the [[causal-state-edm-ood-isomorphism]] synthesis. The claim:

- A **consolidating** paper is in-distribution with respect to the field's current epsilon machine. The compiled base absorbs it with no schema change.
- A **disruptive** paper is an OOD event that forces the compiled base's causal state to split. The compiled base must mint a new node and re-route its edges.
- A **simultaneous discovery** (two independent paths to the same new state) is detectable from future-vector clustering alone — no author lists or explicit mutual citations required.

The [[edm-framework|EDM disruption score]] Δ is the operational signal: high Δ at a node means its neighborhood in the compiled base needs re-evaluation. The [[zettelkasten-engine]] uses this as a curation heuristic — low Δ is redundant, high Δ is flagged for attention, convergent future-vectors are merged.

**Practical implications for the vault:**

- [[mop-edm-cognitive-architecture|MOP-EFHF]] treats the compiled base as a state machine. Agents seeking high β (state entropy) hunt for high-Δ events. MOP provides the policy; EDM provides the measurement; EFHF provides the verification that the high-Δ event is controlled exploration, not hallucination.
- The [[bounded-structured-memory]] synthesis makes the failure mode explicit: each entity carries a `logical.contradictions` field, and ingest-time sanitization rejects garbage that would otherwise inject absorbing states into the causal-state machine.

## PKC in Agent World Models

Beyond static knowledge bases, PKC is now a load-bearing primitive in **agent cognitive world models** for text-based agents (see [[cognitive-world-models-for-llm-agents]]). The four-layer model positions PKC at Layer 3 (World Dynamics Model):

1. **Conversation State (Layer 1)** — traversable belief graph
2. **Tool History Graph (Layer 2)** — state-delta records for every tool call
3. **World Dynamics Model (Layer 3)** — task-specific transition function learned from experience, **requiring PKC for recurring patterns** (unlike physical dynamics, which are universal)
4. **Uncertainty Tracking (Layer 4)** — stale beliefs, user intent drift, plan misprediction via [[epistemic-energy]] depletion

In this framing, the compiled base is the agent's *learned* dynamics — what works, what doesn't, in this domain. Raw context is the *source of truth*; the compiled world model is the *efficient queryable layer* above it. This is the dynamic, session-bound version of PKC: not just "build a wiki" but "an agent that continuously compiles its own operating model."

## Contradictions: A Concrete Answer

The original Open Question — "How to handle contradictions between sources without human arbitration?" — has a concrete, in-vault answer: [[bounded-structured-memory]]'s SSL schema and the [[mop-edm-cognitive-architecture|MOP-EFHF stack]].

**Mechanism:**

1. Each entity carries `logical.contradictions: [String]` — known conflicting entities or facts are first-class fields, not orphans.
2. The [[sheaf-consistency-enforcer]] monitors coboundary norms between agents' beliefs. When two agents disagree about a fact, the edge residual rises; dual pressure accumulates; closure status degrades from KERNEL1 toward WARNING/TIMEOUT.
3. [[mcp-logic]] runs consistency checks: `find_counterexample` with the contested claims as premises and `$F` as conclusion. A model is found → the contradiction is genuinely consistent (both can hold). No model → one of the claims must be revised.
4. The agent's `divergence_signal` (in [[markovian-carryover]]) lights up when reasoning is going off-rails, prompting a re-check.

This is **not** automatic resolution — it's automatic *detection* with formal verification of whether the contradiction is genuine. A genuine contradiction produces a contradicting edge in the graph (not a forced reconciliation), and the agent continues with a flagged, traceable inconsistency rather than silently picking a side. The audit trail is the compiled artifact's distinguishing feature.

**Memory Curse caveat (2605.08060):** Storing *more* contradictions is not always better. The paper shows that expanded recall degrades cooperative intent, and the fix is content curation at ingest — not post-hoc cleanup. The practical rule: cap the `contradictions` field; surface the highest-impact ones; let the rest decay. Naive accumulation of contradictions is the inverse of PKC — it re-introduces RAG's per-query re-derivation cost on every consistency check.

## Compilation Cost vs Query-Time RAG: A Working Answer

The original Open Question — "At what scale does compilation cost exceed query-time RAG cost?" — is answered empirically by this vault's own operation:

**Compilation cost (per source):** ~5 minutes for the [[insights-agent]] pipeline (community detection ~2s + LLM synthesis ~5min + storage ~3s). One source touches 10–15 pages, but the work is bounded and one-shot.

**Query cost:** the [[librarian-agent]] resolves a structured query in seconds over the compiled graph; HITS authority scoring runs in seconds on 1,300+ pages; GAAC clustering in seconds on the full vault.

**The crossover point:** RAG wins on a single query against a corpus the system has never seen. PKC wins on the **Nth query** over the same corpus — the marginal cost of compilation amortizes, and the marginal benefit of a pre-digested answer grows linearly with query volume. The [[llm-wiki-pattern|LLM
```

### → tool_call `mcp__project-synapse__wiki_write_page`

```json
{"path":"wiki/concepts/persistent-knowledge-compilation.md","body":"---\ncreated: 2026-04-07\nupdated: 2026-08-15\ntype: concept\nsummary: Paradigm of LLM pre-compiling knowledge into persistent structured bases vs stateless RAG — with EDM-driven invalidation, agent-world-model role, and answered open questions\ntags: knowledge-management, rag, architecture, core-concept, edm-invalidation, agent-world-models, mop-efhf\nstatus: active\nconfidence: 0.9\nsources:\n  - wiki/sources/articles/llm-wiki-pattern.md\n  - wiki/sources/papers/kim-ahn-edm-2026.md\n  - wiki/sources/papers/ramirez-ruiz-mop-2024.md\n  - wiki/synthesis/causal-state-edm-ood-isomorphism.md\n  - wiki/synthesis/bounded-structured-memory.md\n---\n\n# Persistent Knowledge Compilation\n\nA paradigm shift from stateless [[RAG]] to **compile-time knowledge synthesis**.\n\n## Definition\n\nRather than re-deriving answers from raw documents on every query ([[rag|RAG]]), an LLM pre-processes sources into a persistent, structured knowledge base. The synthesis happens once at ingest time and is kept current — not re-derived on every query. The compiled state is the artifact; the LLM is the compiler.\n\n## Key Properties\n\n- **Accumulative** — each source ingested enriches the existing knowledge. Connections compound; an Nth source costs less than the first because the schema is already in place.\n- **Persistent** — written to disk as Markdown/graph nodes. Survives across sessions, model swaps, and tool reconfigurations.\n- **Pre-digested** — query-time latency drops because the LLM queries structured summaries, not raw text. The expensive work is amortized.\n- **Transparent** — the compiled state is human-readable (Markdown files, graph visualization). A human auditor can inspect what the system \"knows.\"\n\n## Analogies\n\n- Compiler vs. interpreter: [[rag]] interprets on every run; this pattern compiles once.\n- Vannevar Bush's [[memex]] (1945): private, curated knowledge store with associative trails.\n- The difference between a search engine and an encyclopedia.\n- Difference between re-reading the source code and running a compiled binary.\n\n## Implementations\n\n- [[llm-wiki-pattern]] — Karpathy's file-system-only approach (Obsidian + index.md + log.md)\n- [[project-synapse]] — graph-backed implementation with [[neo4j]] + vector search + [[zettelkasten-engine]] for autonomous insight generation\n- This vault ([[wiki/index]]) — a live example: 1,326 pages, 23+ synthesis pages, ~6% stub rate across ~5 months of operation by a single creator plus a [[librarian-agent]] / [[researcher-agent]] / [[insights-agent]] agent sheet\n\n## Invalidation and the EDM Lens\n\nPKC has a built-in failure mode: a compiled knowledge base **goes stale**. A new paper appears that contradicts a load-bearing claim. A concept is split by a discovery. A field shifts vocabulary and the old terms become opaque.\n\nThe formal treatment is in [[edm-framework]] and the [[causal-state-edm-ood-isomorphism]] synthesis. The claim:\n\n- A **consolidating** paper is in-distribution with respect to the field's current epsilon machine. The compiled base absorbs it with no schema change.\n- A **disruptive** paper is an OOD event that forces the compiled base's causal state to split. The compiled base must mint a new node and re-route its edges.\n- A **simultaneous discovery** (two independent paths to the same new state) is detectable from future-vector clustering alone — no author lists or explicit mutual citations required.\n\nThe [[edm-framework|EDM disruption score]] Δ is the operational signal: high Δ at a node means its neighborhood in the compiled base needs re-evaluation. The [[zettelkasten-engine]] uses this as a curation heuristic — low Δ is redundant, high Δ is flagged for attention, convergent future-vectors are merged.\n\n**Practical implications for the vault:**\n\n- [[mop-edm-cognitive-architecture|MOP-EFHF]] treats the compiled base as a state machine. Agents seeking high β (state entropy) hunt for high-Δ events. MOP provides the policy; EDM provides the measurement; EFHF provides the verification that the high-Δ event is controlled exploration, not hallucination.\n- The [[bounded-structured-memory]] synthesis makes the failure mode explicit: each entity carries a `logical.contradictions` field, and ingest-time sanitization rejects garbage that would otherwise inject absorbing states into the causal-state machine.\n\n## PKC in Agent World Models\n\nBeyond static knowledge bases, PKC is now a load-bearing primitive in **agent cognitive world models** for text-based agents (see [[cognitive-world-models-for-llm-agents]]). The four-layer model positions PKC at Layer 3 (World Dynamics Model):\n\n1. **Conversation State (Layer 1)** — traversable belief graph\n2. **Tool History Graph (Layer 2)** — state-delta records for every tool call\n3. **World Dynamics Model (Layer 3)** — task-specific transition function learned from experience, **requiring PKC for recurring patterns** (unlike physical dynamics, which are universal)\n4. **Uncertainty Tracking (Layer 4)** — stale beliefs, user intent drift, plan misprediction via [[epistemic-energy]] depletion\n\nIn this framing, the compiled base is the agent's *learned* dynamics — what works, what doesn't, in this domain. Raw context is the *source of truth*; the compiled world model is the *efficient queryable layer* above it. This is the dynamic, session-bound version of PKC: not just \"build a wiki\" but \"an agent that continuously compiles its own operating model.\"\n\n## Contradictions: A Concrete Answer\n\nThe original Open Question — \"How to handle contradictions between sources without human arbitration?\" — has a concrete, in-vault answer: [[bounded-structured-memory]]'s SSL schema and the [[mop-edm-cognitive-architecture|MOP-EFHF stack]].\n\n**Mechanism:**\n\n1. Each entity carries `logical.contradictions: [String]` — known conflicting entities or facts are first-class fields, not orphans.\n2. The [[sheaf-consistency-enforcer]] monitors coboundary norms between agents' beliefs. When two agents disagree about a fact, the edge residual rises; dual pressure accumulates; closure status degrades from KERNEL1 toward WARNING/TIMEOUT.\n3. [[mcp-logic]] runs consistency checks: `find_counterexample` with the contested claims as premises and `$F` as conclusion. A model is found → the contradiction is genuinely consistent (both can hold). No model → one of the claims must be revised.\n4. The agent's `divergence_signal` (in [[markovian-carryover]]) lights up when reasoning is going off-rails, prompting a re-check.\n\nThis is **not** automatic resolution — it's automatic *detection* with formal verification of whether the contradiction is genuine. A genuine contradiction produces a contradicting edge in the graph (not a forced reconciliation), and the agent continues with a flagged, traceable inconsistency rather than silently picking a side. The audit trail is the compiled artifact's distinguishing feature.\n\n**Memory Curse caveat (2605.08060):** Storing *more* contradictions is not always better. The paper shows that expanded recall degrades cooperative intent, and the fix is content curation at ingest — not post-hoc cleanup. The practical rule: cap the `contradictions` field; surface the highest-impact ones; let the rest decay. Naive accumulation of contradictions is the inverse of PKC — it re-introduces RAG's per-query re-derivation cost on every consistency check.\n\n## Compilation Cost vs Query-Time RAG: A Working Answer\n\nThe original Open Question — \"At what scale does compilation cost exceed query-time RAG cost?\" — is answered empirically by this vault's own operation:\n\n**Compilation cost (per source):** ~5 minutes for the [[insights-agent]] pipeline (community detection ~2s + LLM synthesis ~5min + storage ~3s). One source touches 10–15 pages, but the work is bounded and one-shot.\n\n**Query cost:** the [[librarian-agent]] resolves a structured query in seconds ove
```

### ← tool_result `mcp__project-synapse__wiki_write_page` (55 chars)

```
Wrote wiki/concepts/persistent-knowledge-compilation.md
```

### → tool_call `mcp__project-synapse__wiki_read_page`

```json
{"path":"wiki/concepts/persistent-knowledge-compilation.md"}
```

### ← tool_result `mcp__project-synapse__wiki_read_page` (18450 chars)

```
**Metadata:**
  summary: Paradigm of LLM pre-compiling knowledge into persistent structured bases vs stateless RAG — with EDM-driven invalidation, agent-world-model role, and answered open questions
  tags: ['knowledge-management', 'rag', 'architecture', 'core-concept', 'edm-invalidation', 'agent-world-models', 'mop-efhf']
  updated: 2026-06-03T06:19:36Z

**Content:**
---
created: 2026-04-07
updated: 2026-08-15
type: concept
summary: Paradigm of LLM pre-compiling knowledge into persistent structured bases vs stateless RAG — with EDM-driven invalidation, agent-world-model role, and answered open questions
tags: knowledge-management, rag, architecture, core-concept, edm-invalidation, agent-world-models, mop-efhf
status: active
confidence: 0.9
sources:
  - wiki/sources/articles/llm-wiki-pattern.md
  - wiki/sources/papers/kim-ahn-edm-2026.md
  - wiki/sources/papers/ramirez-ruiz-mop-2024.md
  - wiki/synthesis/causal-state-edm-ood-isomorphism.md
  - wiki/synthesis/bounded-structured-memory.md
---

# Persistent Knowledge Compilation

A paradigm shift from stateless [[RAG]] to **compile-time knowledge synthesis**.

## Definition

Rather than re-deriving answers from raw documents on every query ([[rag|RAG]]), an LLM pre-processes sources into a persistent, structured knowledge base. The synthesis happens once at ingest time and is kept current — not re-derived on every query. The compiled state is the artifact; the LLM is the compiler.

## Key Properties

- **Accumulative** — each source ingested enriches the existing knowledge. Connections compound; an Nth source costs less than the first because the schema is already in place.
- **Persistent** — written to disk as Markdown/graph nodes. Survives across sessions, model swaps, and tool reconfigurations.
- **Pre-digested** — query-time latency drops because the LLM queries structured summaries, not raw text. The expensive work is amortized.
- **Transparent** — the compiled state is human-readable (Markdown files, graph visualization). A human auditor can inspect what the system "knows."

## Analogies

- Compiler vs. interpreter: [[rag]] interprets on every run; this pattern compiles once.
- Vannevar Bush's [[memex]] (1945): private, curated knowledge store with associative trails.
- The difference between a search engine and an encyclopedia.
- Difference between re-reading the source code and running a compiled binary.

## Implementations

- [[llm-wiki-pattern]] — Karpathy's file-system-only approach (Obsidian + index.md + log.md)
- [[project-synapse]] — graph-backed implementation with [[neo4j]] + vector search + [[zettelkasten-engine]] for autonomous insight generation
- This vault ([[wiki/index]]) — a live example: 1,326 pages, 23+ synthesis pages, ~6% stub rate across ~5 months of operation by a single creator plus a [[librarian-agent]] / [[researcher-agent]] / [[insights-agent]] agent sheet

## Invalidation and the EDM Lens

PKC has a built-in failure mode: a compiled knowledge base **goes stale**. A new paper appears that contradicts a load-bearing claim. A concept is split by a discovery. A field shifts vocabulary and the old terms become opaque.

The formal treatment is in [[edm-framework]] and the [[causal-state-edm-ood-isomorphism]] synthesis. The claim:

- A **consolidating** paper is in-distribution with respect to the field's current epsilon machine. The compiled base absorbs it with no schema change.
- A **disruptive** paper is an OOD event that forces the compiled base's causal state to split. The compiled base must mint a new node and re-route its edges.
- A **simultaneous discovery** (two independent paths to the same new state) is detectable from future-vector clustering alone — no author lists or explicit mutual citations required.

The [[edm-framework|EDM disruption score]] Δ is the operational signal: high Δ at a node means its neighborhood in the compiled base needs re-evaluation. The [[zettelkasten-engine]] uses this as a curation heuristic — low Δ is redundant, high Δ is flagged for attention, convergent future-vectors are merged.

**Practical implications for the vault:**

- [[mop-edm-cognitive-architecture|MOP-EFHF]] treats the compiled base as a state machine. Agents seeking high β (state entropy) hunt for high-Δ events. MOP provides the policy; EDM provides the measurement; EFHF provides the verification that the high-Δ event is controlled exploration, not hallucination.
- The [[bounded-structured-memory]] synthesis makes the failure mode explicit: each entity carries a `logical.contradictions` field, and ingest-time sanitization rejects garbage that would otherwise inject absorbing states into the causal-state machine.

## PKC in Agent World Models

Beyond static knowledge bases, PKC is now a load-bearing primitive in **agent cognitive world models** for text-based agents (see [[cognitive-world-models-for-llm-agents]]). The four-layer model positions PKC at Layer 3 (World Dynamics Model):

1. **Conversation State (Layer 1)** — traversable belief graph
2. **Tool History Graph (Layer 2)** — state-delta records for every tool call
3. **World Dynamics Model (Layer 3)** — task-specific transition function learned from experience, **requiring PKC for recurring patterns** (unlike physical dynamics, which are universal)
4. **Uncertainty Tracking (Layer 4)** — stale beliefs, user intent drift, plan misprediction via [[epistemic-energy]] depletion

In this framing, the compiled base is the agent's *learned* dynamics — what works, what doesn't, in this domain. Raw context is the *source of truth*; the compiled world model is the *efficient queryable layer* above it. This is the dynamic, session-bound version of PKC: not just "build a wiki" but "an agent that continuously compiles its own operating model."

## Contradictions: A Concrete Answer

The original Open Question — "How to handle contradictions between sources without human arbitration?" — has a concrete, in-vault answer: [[bounded-structured-memory]]'s SSL schema and the [[mop-edm-cognitive-architecture|MOP-EFHF stack]].

**Mechanism:**

1. Each entity carries `logical.contradictions: [String]` — known conflicting entities or facts are first-class fields, not orphans.
2. The [[sheaf-consistency-enforcer]] monitors coboundary norms between agents' beliefs. When two agents disagree about a fact, the edge residual rises; dual pressure accumulates; closure status degrades from KERNEL1 toward WARNING/TIMEOUT.
3. [[mcp-logic]] runs consistency checks: `find_counterexample` with the contested claims as premises and `$F` as conclusion. A model is found → the contradiction is genuinely consistent (both can hold). No model → one of the claims must be revised.
4. The agent's `divergence_signal` (in [[markovian-carryover]]) lights up when reasoning is going off-rails, prompting a re-check.

This is **not** automatic resolution — it's automatic *detection* with formal verification of whether the contradiction is genuine. A genuine contradiction produces a contradicting edge in the graph (not a forced reconciliation), and the agent continues with a flagged, traceable inconsistency rather than silently picking a side. The audit trail is the compiled artifact's distinguishing feature.

**Memory Curse caveat (2605.08060):** Storing *more* contradictions is not always better. The paper shows that expanded recall degrades cooperative intent, and the fix is content curation at ingest — not post-hoc cleanup. The practical rule: cap the `contradictions` field; surface the highest-impact ones; let the rest decay. Naive accumulation of contradictions is the inverse of PKC — it re-introduces RAG's per-query re-derivation cost on every consistency check.

## Compilation Cost vs Query-Time RAG: A Working Answer

The original Open Question — "At what scale does compilation cost exceed query-time RAG cost?" — is answered empirically by this vault's own operation:

**Compilation cost (per source):** ~5 minutes for the [[insights-agent]] pipeline (community detec
```

### → tool_call `mcp__project-synapse__wiki_lint`

```json
{}
```

### ← tool_result `mcp__project-synapse__wiki_lint` (527951 chars)

```
🩺 **Wiki Health Check** — 1326 pages

**Orphans** (135):
  - discovery-2026-06-01
  - iran-war-day-93-lebanon-incursion-may-31-2026
  - image-extender-readme
  - structural-reuse-unit-of-trustworthiness-2026-06-01
  - applied-mathematics
  - arxiv-2605-28807-calibrating-conservatism-scalable-oversight
  - mop-agents-integration
  - hermes-agent-lcm-plugin-slash-commands-research
  - agem-minimax-m3-copenhagen-iter5
  - orchestrate-teams-claude-code-sessions
  - arxiv-2605-23887-chronos
  - arxiv-2026-06-01-top-papers
  - metropolis-hastings-navarro-2026
  - microsoft-search-magnifica-humanitas-may-2026
  - ingest-2026-05-28
  - vault
  - meta-advancement
  - dflash-block-diffusion-inference-insight
  - discovery-2026-06-02
  - locally-coherent-globally-incoherent-2026
  - ingest-2026-06-01
  - discovery-2026-09-07
  - arxiv-2605-27355-alignment-tampering
  - agem-corpus-1-hard-problem
  - arxiv-2026-05-30-top-papers
  - arxiv-2605-28814-bidirectional-evolutionary-search-bes
  - batch-progress
  - headlines-2026-06-01
  - ingest-2026-06-02
  - news-google-microsoft-pope-leo-ai-encyclical-may-2026
  - librarian
  - github-hermes-agent-lcm-slash-commands-search
  - openai-pope-leo-magnifica-humanitas-may-2026
  - bidirectional-evolutionary-search-bes-2026
  - overseer-2026-06-01
  - sodalitium-pope-leo-xiv-insight
  - speculative-decoding-agent-efficiency-insight
  - air-france-travel-security-focal-point-insight
  - arxiv-2605-27140-stepopsd
  - skillharm-lifecycle-skill-attacks-2026
  - rim-reasoning-in-memory-2026
  - magnifica-humanitas-wikipedia-may-2026
  - audit-2026-05-31
  - ingest
  - mixture-of-recursions
  - clipping_ebola_kenya
  - discovery-2026-08-10
  - arxiv-2605-27354-saerl-sparse-autoencoder-rl
  - hll-humanitys-last-line-verification-2026
  - overseer-2026-06-02
  - arxiv-2605-28816-gamma-world
  - headlines-2026-05-28
  - euler-formula-rope-reasoning-topology-insight
  - microsoft-ai-blog-may-2026
  - discovery-2026-09-09
  - headlines-2026-05-29
  - discovery-2026-09-13
  - moe-sieve-routing-guided-lora-for-efficient-moe-fine-tuning
  - agem-minimax-m27-physics-corpus
  - headlines-2026-05-31
  - discovery-2026-08-21
  - cyprus-flotilla-diplomatic-crisis-insight
  - ai-development-religious-ethics-convergence-insight
  - overseer-2026-05-27
  - headlines-2026-06-02
  - on-the-representation-collapse-of-sparse-mixture-of-experts
  - ingest-2026-05-29
  - clipping_lebanon_ceasefire
  - arxiv-2026-05-29-top-papers
  - markovian-carryover-session-synthesis-insight
  - residual-stream-transformer-vm-insight
  - revenue-model
  - agem-cycle-reflexive-honest-messenger
  - google-deepmind-vatican-encyclical-response-may-2026
  - anthropic-chris-olah-magnifica-humanitas-may-2026
  - clipping_anthropic_ipo
  - insight-merge
  - arxiv-2605-27345-matcha
  - overseer-2026-05-30
  - arxiv-2026-06-02-top-papers
  - hermes-agent-github-labels
  - bvd-utilities-infrastructure-insight
  - audit-2026-06-02
  - discovery-2026-09-08
  - overseer-2026-05-31
  - discovery-2026-05-28
  - blue-origin-new-glenn-explosion-may-2026
  - arxiv-2605-27333-finharness
  - discovery-2026-09-02
  - agent-safety-rl-2026-06-01
  - memo-memory-as-a-model
  - ghana-anti-lgbt-bill-may-2026
  - orcaid
  - TEMPLATE
  - insights
  - paper2code-enhanced
  - hermes-agent
  - reasoning-with-sampling-cutting-at-decision-points-2026
  - clipping_brazil_tariff
  - goodrobot
  - ebola-drc-aid-collapse-convergence-insight
  - vault-2026-05-30
  - audit-2026-08-12
  - wiki-overseer
  - discovery-2026-08-19
  - arxiv-2605-27322-semantic-gradients-interactions-ssd
  - project-synapse-mcp-tools
  - nous-portal-obsidian-para-integration-insight
  - arxiv-2026-05-28-top-papers
  - monitoring-agentic-systems-reliability-2026
  - arxiv-agent
  - discovery-2026-09-12
  - librarians-assistant
  - overseer-2026-05-28
  - llmsurgeon-diagnosing-data-mixture-2026
  - mcp-model-context-protocol-hermes
  - state-scout
  - server-session-unifies-agent-memory-insight
  - entity-tracking-externalization
  - arxiv
  - ingest-2026-05-30
  - clipping_russia_kyiv
  - gamma-world-multi-agent-world-modeling-2026
  - headlines-2026-05-30
  - Meta-Meta Process for Structured Exploration
  - arxiv-2605-10878-kolmogorov-weight-norm
  - discovery-2026-08-20
  - audit-2026-06-01
  - structural-reuse-crosslink-survey-2026-06-01
  - reuserl-orphan-fix-2026-06-01
  - overseer-2026-05-29
  - discovery-2025-09-02
  - news-microsoft-com-may-2026
  - researcher
  - hilbert-manifold-structures-path-spaces
**Broken links** (5857):
  - wiki/concept-index.md → [[wiki/index]]
  - wiki/concept-index.md → [[synthesis/wiki-indexing-theory]]
  - wiki/concept-index.md → [[concepts/maximum-occupancy-principle]]
  - wiki/concept-index.md → [[wiki/index]]
  - wiki/concepts/3dgs.md → [[sources/papers/recuriosity-episodic-context-3d-exploration-2026]]
  - wiki/concepts/CRI.md → [[concepts/cri]]
  - wiki/concepts/CRI.md → [[concepts/maximum-occupancy-principle]]
  - wiki/concepts/CRI.md → [[scratchpad/agent-sheets/librarian/carryover]]
  - wiki/concepts/CRI.md → [[sources/papers/deltabox-stateful-agent-checkpoint-rollback-2026]]
  - wiki/concepts/CRI.md → [[wiki/index]]
  - wiki/concepts/Firecracker.md → [[concepts/firecracker]]
  - wiki/concepts/Firecracker.md → [[wiki/index]]
  - wiki/concepts/Firecracker.md → [[concepts/maximum-occupancy-principle]]
  - wiki/concepts/Firecracker.md → [[sources/papers/deltabox-stateful-agent-checkpoint-rollback-2026]]
  - wiki/concepts/MCTS.md → [[concepts/mcts]]
  - wiki/concepts/MCTS.md → [[scratchpad/jobs/reports/researcher/discovery-2026-06-09]]
  - wiki/concepts/MCTS.md → [[concepts/causal-reasoning]]
  - wiki/concepts/MCTS.md → [[wiki/index]]
  - wiki/concepts/MCTS.md → [[concepts/collm-nas]]
  - wiki/concepts/MCTS.md → [[sources/papers/deltabox-stateful-agent-checkpoint-rollback-2026]]
  - wiki/concepts/MCTS.md → [[concepts/code-generation]]
  - wiki/concepts/MCTS.md → [[concepts/swe-bench]]
  - wiki/concepts/MCTS.md → [[concepts/process-reward-model]]
  - wiki/concepts/MCTS.md → [[concepts/world-model]]
  - wiki/concepts/MCTS.md → [[concepts/agentic-hierarchy]]
  - wiki/concepts/absence-of-worst-case-metric.md → [[concepts/spin-vs-substrate]]
  - wiki/concepts/absence-of-worst-case-metric.md → [[concepts/frame-transmission]]
  - wiki/concepts/absence-of-worst-case-metric.md → [[synthesis/ai-governance-substrate-analysis]]
  - wiki/concepts/absence-of-worst-case-metric.md → [[wiki/index]]
  - wiki/concepts/absence-of-worst-case-metric.md → [[concepts/absence-of-worst-case-metric]]
  - wiki/concepts/absence-of-worst-case-metric.md → [[sources/papers/deepweb-bench-2026]]
  - wiki/concepts/absence-of-worst-case-metric.md → [[concepts/institutional-capture-vs-species-framing]]
  - wiki/concepts/abstract-algebra.md → [[concepts/algebra]]
  - wiki/concepts/abstract-algebra.md → [[concepts/pure-mathematics]]
  - wiki/concepts/abstract-algebra.md → [[wiki/index]]
  - wiki/concepts/abstract-algebra.md → [[concepts/abstract-algebra]]
  - wiki/concepts/academic-peer-review.md → [[concepts/critical-review-scientific-article-guide]]
  - wiki/concepts/academic-peer-review.md → [[sources/articles/critical-review-scientific-article-guide]]
  - wiki/concepts/academic-peer-review.md → [[wiki/index]]
  - wiki/concepts/academic-peer-review.md → [[concepts/critical-analysis]]
  - wiki/concepts/academic-peer-review.md → [[concepts/research-methodology]]
  - wiki/concepts/academic-peer-review.md → [[concepts/scientific-writing]]
  - wiki/concepts/academic-peer-review.md → [[concepts/academic-peer-review]]
  - wiki/concepts/academic-writing.md → [[wiki/index]]
  - wiki/concepts/academic-writing.md → [[concepts/academic-writing]]
  - wiki/concepts/academic-writing.md → [[concepts/scientific-writing]]
  - wiki/concepts/academic-writing.md → [[concepts/research-paper]]
  - wiki/concepts/accountability.md → [[concepts/institutional-capture]]
  - wiki/concepts/accountability.md → [[concepts/governance]]
  - wiki/conce
```


## Run end

```json
{
  "turns": 5,
  "elapsedSeconds": 197
}
```

